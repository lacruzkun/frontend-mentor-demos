# QR CODE COMPONENT

This is my first anything web dev so 
I used divs to group the whole component
and then i used classes to style the tags in the CSS
also made the image follow the width of the container its in using
